import StyleSheet from 'StyleSheet'

const styles = StyleSheet.create({
  backButton: {
    marginLeft: 15,
    width: 50,
    marginTop: -6
  },
  title: {
    fontSize: 18
  }
})

export default styles
